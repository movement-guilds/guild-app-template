-- APP_SCHEMA placeholder — replaced by bootstrap script with the app's schema prefix (e.g. gc, gl)
-- DO NOT edit APP_SCHEMA manually — run ./scripts/new-app.sh <app-name> <schema-prefix> instead

CREATE SCHEMA IF NOT EXISTS APP_SCHEMA;
